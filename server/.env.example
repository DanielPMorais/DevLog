# Configurações do Servidor
PORT=3003
NODE_ENV=production

# Banco de Dados
DB_PATH=./data/devlog.db

# CORS - URLs permitidas (separar por vírgula se múltiplas)
CORS_ORIGIN=http://10.0.0.126,http://localhost:5173
